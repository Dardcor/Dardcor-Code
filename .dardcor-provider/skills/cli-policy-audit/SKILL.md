---
name: cli-policy-audit
description: Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.

## Quick install

```bash
npm install -g Dardcor Code   # or: npx Dardcor Code
Dardcor Code --version
```

## Subcommands

### `audit`

**Example:**

```bash
Dardcor Code audit
```

### `audit tail`

**Flags:**

- `--source <s>`
- `--follow`
- `--limit <n>`

**Example:**

```bash
Dardcor Code audit tail
```

### `audit search <query>`

**Flags:**

- `--source <s>`
- `--since <ts>`
- `--until <ts>`
- `--limit <n>`
- `--actor <id>`
- `--action <a>`

**Example:**

```bash
Dardcor Code audit search <query>
```

### `audit export <file>`

**Flags:**

- `--source <s>`
- `--format <f>`
- `--since <ts>`
- `--until <ts>`

**Example:**

```bash
Dardcor Code audit export <file>
```

### `audit stats`

**Flags:**

- `--source <s>`
- `--period <p>`

**Example:**

```bash
Dardcor Code audit stats
```

### `audit get <id>`

**Flags:**

- `--source <s>`

**Example:**

```bash
Dardcor Code audit get <id>
```

### `logs`

**Flags:**

- `--follow`
- `--filter <level>`
- `--lines <n>`
- `--timeout <ms>`
- `--base-url <url>`
- `--request-id <id>`
- `--api-key <key>`
- `--combo <name>`
- `--status <code>`
- `--duration-min <ms>`
- `--duration-max <ms>`
- `--export <path>`

**Example:**

```bash
Dardcor Code logs
```

### `policy`

**Example:**

```bash
Dardcor Code policy
```

### `policy list`

**Flags:**

- `--kind <k>`
- `--scope <s>`

**Example:**

```bash
Dardcor Code policy list
```

### `policy get <id>`

**Example:**

```bash
Dardcor Code policy get <id>
```

### `policy create`

**Example:**

```bash
Dardcor Code policy create
```

### `policy update <id>`

**Example:**

```bash
Dardcor Code policy update <id>
```

### `policy delete <id>`

**Flags:**

- `--yes`

**Example:**

```bash
Dardcor Code policy delete <id>
```

### `policy evaluate`

**Flags:**

- `--resource <r>`
- `--context <json>`

**Example:**

```bash
Dardcor Code policy evaluate
```

### `policy export <file>`

**Example:**

```bash
Dardcor Code policy export <file>
```

### `policy import <file>`

**Flags:**

- `--overwrite`

**Example:**

```bash
Dardcor Code policy import <file>
```

### `telemetry`

**Example:**

```bash
Dardcor Code telemetry
```

### `telemetry summary`

**Flags:**

- `--period <p>`
- `--compare-to <p>`

**Example:**

```bash
Dardcor Code telemetry summary
```

### `telemetry export`

**Flags:**

- `--out <path>`
- `--period <p>`

**Example:**

```bash
Dardcor Code telemetry export
```
