---
name: omni-mcp
description: "Connect to the Dardcor Code MCP server (37 tools, 3 transports: SSE/stdio/HTTP). Covers routing, cache, compression, memory, skills, providers, and audit tools across 16 permission scopes."
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Connect to the Dardcor Code MCP server (37 tools, 3 transports: SSE/stdio/HTTP). Covers routing, cache, compression, memory, skills, providers, and audit tools across 16 permission scopes.

## Authentication

All requests require a valid Bearer token or session cookie. Obtain a token via `POST /api/auth/login` or configure `REQUIRE_API_KEY=false` for local development.

## Endpoints

_No endpoints mapped for this area yet._
## Payloads

See the full OpenAPI specification at `GET /api/openapi/spec` or `docs/openapi.yaml` for detailed request/response schemas.

<!-- skill:custom-start -->
<!-- Migrated from skills/Dardcor Code-mcp/SKILL.md (preserved curated content) -->

# Dardcor Code — MCP Server

Requires `OMNIROUTE_URL` and `OMNIROUTE_KEY`. See [entry-point SKILL](https://raw.githubusercontent.com/diegosouzapw/Dardcor Code/main/skills/Dardcor Code/SKILL.md) for setup.

## Transports

- **stdio** — local IPC, for Claude Desktop / VS Code extensions
- **SSE** — `GET $OMNIROUTE_URL/api/mcp/sse`
- **Streamable HTTP** — `POST $OMNIROUTE_URL/api/mcp/stream`

## Claude Desktop config

Add to `~/Library/Application Support/Claude/claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "Dardcor Code": {
      "command": "npx",
      "args": ["-y", "Dardcor Code", "--mcp"],
      "env": { "OMNIROUTE_KEY": "sk-..." }
    }
  }
}
```

## Cursor / VS Code config

```json
{
  "mcp": {
    "servers": {
      "Dardcor Code": {
        "url": "http://localhost:20128/api/mcp/sse",
        "headers": { "Authorization": "Bearer sk-..." }
      }
    }
  }
}
```

## Available tools (37 total)

| Scope     | Tools                                                                                              |
| --------- | -------------------------------------------------------------------------------------------------- |
| health    | `Dardcor Code_get_health`                                                                             |
| combos    | `Dardcor Code_list_combos`, `Dardcor Code_get_combo_metrics`, `Dardcor Code_switch_combo`                   |
| routing   | `Dardcor Code_simulate_route`, `Dardcor Code_best_combo_for_task`, `Dardcor Code_explain_route`             |
| providers | `Dardcor Code_get_provider_metrics`, `Dardcor Code_check_quota`, `Dardcor Code_route_request`               |
| budget    | `Dardcor Code_set_budget_guard`, `Dardcor Code_set_routing_strategy`, `Dardcor Code_set_resilience_profile` |
| testing   | `Dardcor Code_test_combo`                                                                             |
| memory    | `memory_add`, `memory_search`, `memory_delete`                                                     |
| skills    | `skill_invoke`, `skill_list`, `skill_describe`, `skill_register`                                   |
| cache     | `Dardcor Code_cache_stats`, `Dardcor Code_cache_flush`                                                   |
| admin     | `Dardcor Code_db_health_check`, `Dardcor Code_sync_pricing`, `Dardcor Code_get_session_snapshot`            |

Full list: `GET $OMNIROUTE_URL/api/mcp/tools`

## Scopes

Tools are grouped into 13 scopes (chat-only, memory-readonly, full-admin, etc.).
Pass scope name as `--scope` arg or via `X-Omniroute-Scope` header.

## Reference

Full docs: https://github.com/diegosouzapw/Dardcor Code/blob/main/docs/frameworks/MCP-SERVER.md
<!-- skill:custom-end -->
