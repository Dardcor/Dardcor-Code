trigger: none

pr: none

resources:
  repositories:
    - repository: templates
      type: github
      name: microsoft/vscode-engineering
      ref: main
      endpoint: Monaco
  pipelines:
    - pipeline: vscode
      source: '⭐️ VS Code'
      trigger:
        stages:
          - Release
        branches:
          include:
            - main
            - release/*

parameters:
  - name: NPM_REGISTRY
    displayName: "Custom NPM Registry"
    type: string
    default: 'https://pkgs.dev.azure.com/monacotools/Monaco/_packaging/vscode/npm/registry/'

name: "$(Date:yyyyMMdd).$(Rev:r)"

extends:
  template: azure-pipelines/npm-package/pipeline.yml@templates
  parameters:
    npmPackages:
      - name: vscode-dardcor-chat
        buildSteps:
          - task: NodeTool@0
            inputs:
              versionSpec: 22.x
            displayName: 🛠 Install Node.js (22.x)

          - template: extensions/dardcor/build/steps/setup-npm.yml@self
            parameters:
              NPM_REGISTRY: ${{ parameters.NPM_REGISTRY }}

          - download: vscode
            artifact: dardcor_vsix
            displayName: 📥 Download Copilot VSIX
            condition: and(succeeded(), ne(variables['resources.pipeline.vscode.runName'], ''))

          - bash: |
              set -e
              if [ -f "$(Pipeline.Workspace)/vscode/dardcor_vsix/dardcor-chat.vsix" ]; then
                unzip -o "$(Pipeline.Workspace)/vscode/dardcor_vsix/dardcor-chat.vsix" 'extension/package.json'
                VERSION=$(node -p "require('./extension/package.json').version")
                rm -rf extension
                node -e "
                  const fs = require('fs');
                  const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
                  pkg.version = '$VERSION';
                  fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
                "
                echo "Set extension version to $VERSION"
              else
                echo "No Copilot VSIX found, using version from package.json"
              fi
            displayName: 📋 Sync version from extension
            workingDirectory: extensions/dardcor

          - bash: npm ci && npm run extract-chat-lib && rm -rf node_modules
            displayName: 📂 Extract chat-lib
            workingDirectory: extensions/dardcor
            env:
              PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 1

          - bash: |
              set -e
              VERSION=$(node -p "require('../package.json').version")
              node -e "
                const fs = require('fs');
                const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
                pkg.version = '$VERSION';
                fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
              "
              echo "Set chat-lib version to $VERSION"
            displayName: 📋 Sync chat-lib version
            workingDirectory: extensions/dardcor/chat-lib

          - script: npm ci
            displayName: 📦 Install chat-lib dependencies
            workingDirectory: extensions/dardcor/chat-lib

          - script: npm run build
            displayName: 🔨 Build chat-lib
            workingDirectory: extensions/dardcor/chat-lib
        testPlatforms:
          - name: Linux
            nodeVersions: [22.x]
          - name: MacOS
            nodeVersions: [22.x]
          - name: Windows
            nodeVersions: [22.x]
        workingDirectory: extensions/dardcor/chat-lib
        testSteps:
          - template: extensions/dardcor/build/steps/test-chat-lib.yml@self
            parameters:
              NPM_REGISTRY: ${{ parameters.NPM_REGISTRY }}
        # Triggered by ⭐️ VS Code pipeline → always publish to next
        publishPackage: true
        publishRequiresApproval: false
        tag: next
        ghCreateRelease: false
        ghReleaseAddChangeLog: false
